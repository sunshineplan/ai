name: Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    if: ${{ github.actor != 'dependabot[bot]' }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ windows-latest, ubuntu-latest, macos-latest ]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: stable

      - name: Test Code
        run: |
          go build -v ./...
          go test -v -race ./...

  ai:
    if: ${{ github.actor != 'dependabot[bot]' }}
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: stable

      - name: Test Code
        env: 
          CHATGPT_API_KEY: ${{ secrets.CHATGPT_API_KEY }}
          CHATGPT_PROXY: ${{ vars.CHATGPT_PROXY }}
          CHATGPT_ENDPOINT: ${{ vars.CHATGPT_ENDPOINT }}
          CHATGPT_MODEL: ${{ vars.CHATGPT_MODEL }}
          CHATGPT_MODEL_FOR_IMAGE: ${{ vars.CHATGPT_MODEL_FOR_IMAGE }}
          CHATGPT_MODEL_FOR_TOOLS: ${{ vars.CHATGPT_MODEL_FOR_TOOLS }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_PROXY: ${{ vars.GEMINI_PROXY }}
          GEMINI_ENDPOINT: ${{ vars.GEMINI_ENDPOINT }}
          GEMINI_MODEL: ${{ vars.GEMINI_MODEL }}
          GEMINI_MODEL_FOR_IMAGE: ${{ vars.GEMINI_MODEL_FOR_IMAGE }}
          GEMINI_MODEL_FOR_TOOLS: ${{ vars.GEMINI_MODEL_FOR_TOOLS }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          ANTHROPIC_PROXY: ${{ vars.ANTHROPIC_PROXY }}
          ANTHROPIC_ENDPOINT: ${{ vars.ANTHROPIC_ENDPOINT }}
          ANTHROPIC_MODEL: ${{ vars.ANTHROPIC_MODEL }}
          ANTHROPIC_MODEL_FOR_IMAGE: ${{ vars.ANTHROPIC_MODEL_FOR_IMAGE }}
          ANTHROPIC_MODEL_FOR_TOOLS: ${{ vars.ANTHROPIC_MODEL_FOR_TOOLS }}
        run: go test -v
